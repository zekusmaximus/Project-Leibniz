# Dependencies (each sub-project has its own node_modules)
node_modules/

# Build output
dist/
build/

# Environment variables / secrets — never commit (templates like .env.example are kept)
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*

# Editor / OS cruft
.DS_Store
.idea/
.vscode/
